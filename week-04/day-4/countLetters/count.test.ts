'use strict';
import {test} from 'tape';
import {countLetters} from './count';

test('idk yet', t =>{
let actual = typeof(countLetters('bookies'))
let expected = typeof({})
t.equal(actual,expected)
t.end()
})